[user]
	name = baileyb0t
	email = bailey@hrdag.org

[status]
	submoduleSummary = true
	showUntrackedFiles = all

[color]
	ui = auto

[color "grep"]
   	context = dim white
   	function = dim white
   	filename = dim white
   	linenumber = dim yellow
   	match = red
   	selected = white
   	separator = dim blue

[grep]
	lineNumber = true

[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true

[init]
	defaultBranch = main

[branch "bailey-working"]
	rebase = preserve

[color "branch"]
	upstream = cyan 

[push]
	default = simple

# <------ done ------>
